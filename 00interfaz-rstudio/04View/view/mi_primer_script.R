# Cargamos libreria
library(readr)

# Importamos dataset `centros_acogica.csv`
centros_acogida <- read_csv2("data/centros_acogida.csv")

# Visualizamos el dataframe `centros_acogida`
View(centros_acogida)